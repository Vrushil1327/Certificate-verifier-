# Certificate-verifier-
Colleges issue certificates → stored as transactions on a blockchain.  Certificates cannot be altered once added (immutability).  Anyone can verify certificate ID → blockchain returns whether it is authentic.  New blocks get mined with proof-of-work.  Full chain view for auditors.
